# syfe_Assignment
Selenium automation framework for SauceDemo covering complete user journey (login → cart → checkout → logout) with TestNG, dynamic locators, and stable execution using explicit waits.
